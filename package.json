{
  "name": "sudoku-mvp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "npx serve . -p 5173",
    "cap:init": "npx cap init sudoku-mvp lt.pspasaulis.sudoku --web-dir=.",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "build:android:ci": "npx cap add android || true && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "serve": "^14.2.1"
  }
}