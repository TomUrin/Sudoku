import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lt.pspasaulis.sudoku',
  appName: 'Sudoku MVP',
  webDir: '.',
  server: { androidScheme: 'https' }
};

export default config;
